# Every real route is prerendered to its own index.html by
# scripts/build-routes.mjs, and static files are matched before this rule. What
# reaches it is genuinely nothing, so it answers 404 rather than serving the
# start page under an invented address.
/*    /404.html   404
